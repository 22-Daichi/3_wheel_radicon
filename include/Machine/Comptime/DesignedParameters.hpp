#pragma once

#include <Arduino.h>

namespace Machine::Comptime::DesignedParameters {

constexpr float ANGLE_OFFSET_A = PI;
constexpr float ANGLE_OFFSET_B = -PI / 3;
constexpr float ANGLE_OFFSET_C = PI / 3;

}